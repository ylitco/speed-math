import React, { FC } from 'react';
import styles from '../views.module.scss'

export const MainSettings: FC = () => {
  return (
    <main className={styles.view}>
      <h1>Main Settings View</h1>
    </main>
  );
};
